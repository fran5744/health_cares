<%= form_with(model: user, local: true) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :user_user_id %>
  </div>

  <div class="field">
    <%= form.label :ps %>
    <%= form.text_field :ps, id: :user_ps %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :user_name %>
  </div>

  <div class="field">
    <%= form.label :authority %>
    <%= form.number_field :authority, id: :user_authority %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
